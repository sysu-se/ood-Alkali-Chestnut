# Homework 2 · Hint 与 Explore Mode

本次作业在 Homework 1.1 的基础上，继续演进 `Sudoku` / `Game` 的对象设计，引入 **提示（Hint）** 与 **探索（Explore）** 两个能力，重点关注：  
- 状态切换  
- 历史分支  
- 失败回溯  
- 功能演进对原设计的影响  

---

## 一、提示（Hint）的实现与取舍

### 1. 实现方式
当前实现以 **候选集** 作为提示的核心能力：

- `Sudoku` 提供候选集计算
- `Game` 暴露 `getCandidates(row, col)`
- UI 根据候选集进行提示逻辑（唯一候选则直接填入，否则展示候选）

这种设计保持了：  
- 领域规则在 `Sudoku`  
- 会话逻辑在 `Game`  
- 交互呈现在 UI  

---

### 2. Hint 属于 `Sudoku` 还是 `Game`？

**回答：候选能力属于 `Sudoku`，提示行为由 `Game` 组织。**

原因：  
- 候选集是棋盘约束推导的结果，属于领域模型。  
- “提示”是用户交互行为，不应塞回 Sudoku。  
- `Game` 作为会话对象，承担“把候选变成提示”的职责更合理。  

---

### 3. Hint 的设计取舍与限制
当前提示功能只做到**最小可用版本**，存在以下取舍：

- **未实现“标红格子”的提示**  
  已冲突格（标红格）不能被提示。  
- **未实现“初始为空、后续已填入”的格子的提示**  
  这些格子不能被作为 hint 目标。  
- **没有提示解释理由**  
  只告诉候选，并未说明推理依据。  
- **没有多等级提示**  
  仅提供“候选提示 / 唯一候选填入”。  

以上取舍主要为了保持领域对象简洁、避免引入过多推理逻辑。

---

## 二、探索模式（Explore）的实现与取舍

### 1. 探索模式本质
设计为 **Game 状态切换 + 临时分支会话**：

- 进入探索：  
  - 复制当前棋盘快照 `exploreSnapshot`  
  - 新建探索历史 `exploreHistory`  
- 提交探索：  
  - 若探索盘面变化，则写入主历史  
- 放弃探索：  
  - 回滚到 `exploreSnapshot`  

探索过程与主线历史隔离，避免污染主局面。

---

### 2. 主局面与探索局面的关系
- **不是共享对象**，而是复制（通过 JSON / clone）
- 独立历史，互不影响
- 提交时只写入最终状态

这种结构能避免深拷贝污染，同时避免引入复杂 DAG 历史。

---

### 3. History 的演进
Homework 1 为 **单线性历史**，Homework 2 中扩展为：

- **主历史**：`history / historyIndex`
- **探索历史**：`exploreHistory / exploreHistoryIndex`

探索期间 Undo/Redo 操作只影响探索历史；提交后再回到主历史。

---

## 三、探索失败记忆（Memory）的设计取舍

### 1. 当前实现
当探索失败时，仅记录 **探索的第一个填入值**：

- `_exploreFirstMove`
- `_failedFirstMoves` 记录 `(row, col, value)`  
- UI 弹窗提示探索失败, “确定”保存探索结果，“取消”放弃探索结果  

此外，当再次进入探索模式后，**在之前失败探索路径的第一个格子输入值会弹窗提示**：  

- “确定”继续探索  
- “取消”退出探索  

---

### 2. 为什么只记录首步？
- 记录完整探索路径需要前缀匹配或分支树结构，复杂度明显提升  
- 任务目标强调“记忆失败路径”，首步失败属于一种轻量可解释的剪枝  
- 成本低、实现清晰，可作为演进的第一版  

---

### 3. 当前缺陷
- 首步失败并不意味着所有后续路径必然失败  
- 只能提供“弱提示”，不具备真正的路径剪枝能力  
- 未实现“失败序列完整回放”或“中间路径提示”  

---

## 四、Homework 1 设计暴露的局限

1. **History 只能单线**  
   探索模式必须引入第二条历史。  

2. **缺少状态机概念**  
   进入探索时必须新增状态字段与分支逻辑。  

3. **Hint 需要更多领域支持**  
   如果要做高级提示，`Sudoku` 需要更丰富的推理方法。  

---

## 五、如果重做 Homework 1 的改进方向

- 提前引入 **Session / Mode** 概念  
- 将 History 抽象为 “主线 + 分支”的统一结构  
- 预留 Hint / Solve 的扩展接口  
- 把棋盘推理逻辑模块化（为解释型 Hint 做准备）  

---

## 六、总结

本次 Homework 2 的演进重点在于：

-  `Game` 引入探索状态  
-  增加探索历史与回滚机制  
-  增加探索失败记忆  
-  提示系统通过候选集支撑  
-  保持原 `Sudoku` / `Game` 结构整体一致  

同时保留了明显的扩展空间：

- 探索可扩展为完整路径记忆  
- Hint 可扩展为多层推理或解释式提示  
- History 可升级为真正的分支树  
